# Claude Code
.claude/

# Dependencies
node_modules/

# Local wrangler dev state (D1 sqlite, etc.)
.wrangler/

# Environment variables
.env
.env.local
.env.*.local
.dev.vars

# Logs
logs/
*.log
npm-debug.log*

# OS files
.DS_Store
Thumbs.db

# Editor directories
.vscode/
.idea/
*.swp
*.swo

# Security audit documents
ungcyberunit_security_remediation.md

# Challenge answer keys — content lives in D1 (challenge_answer_keys table),
# never in the repo, since this GitHub repo is public. Source files for
# ingestion (see docs, or ask the assistant) stay local only.
*-answer-key.pdf

# Proposed-but-not-yet-built feature/curriculum plans — kept local until
# acted on. docs/public-profile-plan.md is tracked (already built), so it
# deliberately doesn't match this pattern.
docs/plan-*.md

# Build output (if added later)
dist/
build/
